# The project Id in GCP in which you're performing the installation
project_id = "<PROJECT-ID>"

# The region for the installation (please note that the region has to have access to GPU (A100) instances, as mentioned in the prerequisites section
region = "<REGION>"

# The availability zones in which cluster is deployed
zones = ["<ZONE-1>", "<ZONE-2>"]

# Prefix for all created resource names
prefix = "<PREFIX>"

